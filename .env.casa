# Configurações para ambiente CASA
APP_ENV=development
APP_DEBUG=true
APP_NAME="Portal de Notícias - Casa"
APP_URL=http://localhost:8000

# Banco de Dados
DB_HOST=localhost
DB_NAME=portal_noticias
DB_USER=root
DB_PASS=root
DB_PORT=3306

# Servidor
FRONTEND_PORT=8000
BACKEND_PORT=8080
FRONTEND_URL=http://localhost:8000
BACKEND_URL=http://localhost:8080

# CORS
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Upload
UPLOAD_PATH=backend/uploads
UPLOAD_URL=http://localhost:8080/backend/uploads
UPLOAD_MAX_SIZE=10485760

# JWT
JWT_SECRET=sua_chave_secreta_casa_2024
JWT_EXPIRATION=3600

# Email (MailHog para desenvolvimento)
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=noreply@localhost

# Cache
CACHE_ENABLED=true
CACHE_TTL=3600

# Logs
LOG_LEVEL=debug
LOG_PATH=backend/logs